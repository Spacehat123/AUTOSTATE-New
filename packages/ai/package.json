{
  "name": "@autostate/ai",
  "version": "1.0.0",
  "description": "Model-agnostic AI service package for Autostate — swap providers via env vars",
  "main": "index.ts",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@ai-sdk/gateway": "^1.0.41",
    "@autostate/database": "workspace:*",
    "ai": "^4.3.19",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^4.1.10"
  }
}

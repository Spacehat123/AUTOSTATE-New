/**
 * Re-exports from the @autostate/whatsapp service package.
 * Import from this file within the Next.js app to keep paths clean.
 */
export { sendTextMessage, sendTemplateMessage } from '../../../../../services/whatsapp/index'
